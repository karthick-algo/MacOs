name: Secure User Setup on macOS

on:
  workflow_dispatch:

jobs:
  setup-user:
    runs-on: macos-latest

    steps:
      - name: Set Environment Variables for User
        run: |
          export USERNAME=myuser
          export PASSWORD=mypassword
          echo "Environment variables set successfully."
